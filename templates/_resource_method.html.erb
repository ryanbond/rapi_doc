<div id="method_<%=@method_order%>">
  
  <%
    splitted = @method
    str = @method
    str = "<span id='http_verb'>#{str}</span>"
  %>
  <p class='description'><strong>Description:</strong> <%=@content %></p>

  <h2><%=str %><%=@url %></h2>
  
  <% if @access %>
    <strong>Access:</strong> <%=@access %><br />
  <% end %>

  <% if @return %>
    <strong>Return:</strong>
    <%=@return %>
    <br />
  <% end %>

  <% if @params %>
    <strong>Parameters:</strong>
    <ul>
      <% @params.each do |v| %><li><%=v %></li><% end %>
    </ul>
  <% end %>

  <% unless @outputs.empty? %>
    <ul class="tabs">
      <% @outputs.each do |output_format, output| %>
        <li class="<%= (output_format == @outputs.keys.first)? 'active' : '' %>"><a href='#<%= "#{output_format}_#{@method_order}" %>'><%= output_format %></a></li>
      <% end %>
    </ul>
    <div class="pill-content">
      <% @outputs.each do |output_format, output| %>
        <div class="<%= (output_format == @outputs.keys.first)? 'active' : '' %>" id='<%= "#{output_format}_#{@method_order}" %>'>
          <pre class="prettyprint" ><%= output %></pre>
        </div>
      <% end %>
    </div>
    <br />
  <% end %>

  <% unless @request.blank? %>
    <strong>Request:</strong>
    <pre class="code"><code><%= @request %></code></pre>
  <% end %>

  <% unless @response.blank? %>  
    <strong>Response:</strong>
    <pre class="code"><code><%= @response %></code></pre>
  <% end %>

</div>
